# Sara requirements
# Install only what you need:
#
#   pip install -e ".[rag]"        # RAG + KD-SPAR (no GPU needed)
#   pip install -e ".[vision]"     # Vision distillation (needs GPU)
#   pip install -e ".[nlp]"        # BERT distillation (needs GPU)
#   pip install -e ".[all]"        # Everything
#   pip install -e ".[rag,dev]"    # RAG + tests
#
# For the OryxPro ablation experiment, only .[rag] is needed:
#   pip install -e ".[rag]"

# ── RAG / KD-SPAR (Anthropic + Ollama) ───────────────────────────────────────
anthropic>=0.25
chromadb>=0.5
sentence-transformers>=3.0
requests>=2.31

# ── Vision distillation (optional, requires GPU) ─────────────────────────────
# torch>=2.1
# torchvision>=0.16

# ── NLP / BERT distillation (optional, requires GPU) ────────────────────────
# torch>=2.1
# transformers>=4.40
# datasets>=2.18
# evaluate>=0.4
# accelerate>=0.28

# ── Development / testing ────────────────────────────────────────────────────
# pytest>=8.0
# pytest-cov>=5.0
# ruff>=0.4

# ── Paper generation (on build machine only) ─────────────────────────────────
# reportlab>=4.1
# pypdf>=4.0
